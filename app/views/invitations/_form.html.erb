<%= form_with(model: invitation, local: true) do |form| %>
  <% if invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
        <% invitation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="row">
  <div class="col-6 form-floating mb-3">
  <div class="input-group">
      <div class="input-group-text">Recipient Name</div>
    <%= form.text_field :recipient_name, class:"form-control",placeholder:"Recipent Name" %>  
  </div></div></div>

  <div class="row">
  <div class="col-6 form-floating mb-3">
  <div class="input-group">
      <div class="input-group-text">Recipient Email</div>
    <%= form.text_field :recipient_email, class:"form-control",placeholder:"Recipient Email" %>  
    <%= form.hidden_field :property_id, value: params[:property_id] %>
  </div></div></div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
